# ── Training pipeline dependencies ───────────────────────────────────────────
# Install with:  pip install -r training/requirements-training.txt
#
# Requirements:
#   - Python 3.10 or 3.11  (Unsloth does not support 3.12+)
#   - CUDA 11.8+ and an NVIDIA GPU with 8 GB+ VRAM (16 GB recommended)
#   - Colab (recommended): use the sentra_colab notebook instead
#
# Unsloth must be installed first via its own installer — see:
#   https://docs.unsloth.ai/get-started/installing-+-updating-unsloth/pip-install
# After Unsloth is installed, run this file for the remaining deps.

# Core ML
torch>=2.1.0
transformers>=4.46.0
peft>=0.14.0
trl>=0.12.0
accelerate>=0.26.0
bitsandbytes>=0.41.0

# Datasets
datasets>=2.14.0
huggingface_hub>=0.20.0

# Evaluation
scikit-learn>=1.3.0
matplotlib>=3.7.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0
